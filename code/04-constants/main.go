package main

// import "math/rand"

const userName = "Maximilian"
const age = 64 / 2 // allowed
// const random = rand.Int() // not allowed

const (
	inputAttack = iota + 1
	inputSpecialAttack
	inputHeal
)

func main() {
	// userName = "Max"
}
